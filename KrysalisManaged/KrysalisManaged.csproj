<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<RootNamespace>KrysalisManaged</RootNamespace>
		<AssemblyName>KrysalisManaged</AssemblyName>
		<TargetFramework>net472</TargetFramework>
		<LangVersion>10.0</LangVersion>
		<Nullable>enable</Nullable>
		<Deterministic>true</Deterministic>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<DebugType Condition="'$(Configuration)'=='Debug'">embedded</DebugType>
	</PropertyGroup>
	<Import Project="$(SolutionDir)\Common.props" />	
	<ItemGroup>
		<Reference Include="ResoniteModLoader">
			<HintPath Condition="Exists('$(ResonitePath)Libraries\ResoniteModLoader.dll')">$(ResonitePath)Libraries\ResoniteModLoader.dll</HintPath>
			<HintPath Condition="!Exists('$(ResonitePath)Libraries\ResoniteModLoader.dll')">$(SolutionDir)external\bin\ResoniteModLoader.dll</HintPath>
			<Private>False</Private>
		</Reference>		
		<Reference Include="HarmonyLib">
			<HintPath Condition="Exists('$(ResonitePath)rml_libs\0Harmony.dll')">$(ResonitePath)rml_libs\0Harmony.dll</HintPath>
			<HintPath Condition="!Exists('$(ResonitePath)rml_libs\0Harmony.dll')">$(SolutionDir)external\bin\0Harmony.dll</HintPath>
			<Private>False</Private>
		</Reference>		
		<Reference Include="FrooxEngine">
			<HintPath Condition="Exists('$(ResonitePath)Resonite_Data\Managed\FrooxEngine.dll')">$(ResonitePath)Resonite_Data\Managed\FrooxEngine.dll</HintPath>
			<HintPath Condition="!Exists('$(ResonitePath)Resonite_Data\Managed\FrooxEngine.dll')">$(SolutionDir)external\bin\FrooxEngine.dll</HintPath>
			<Private>False</Private>
		</Reference>
	</ItemGroup>
  <Target Name="CopyDllToBuildDirectory" AfterTargets="Build">
    <Message Text="Copying KrysalisNative.dll to $(SolutionDir)build\Krysalis\" Importance="high" />
    <Copy SourceFiles="$(TargetDir)$(TargetFilename)"
      DestinationFolder="$(SolutionDir)build\" />
  </Target>
</Project>